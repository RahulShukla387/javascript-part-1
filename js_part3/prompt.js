let firstname = prompt("enter the first name "); // but this code will not run in the node js
let secondname = prompt("enter the second name "); // but this code will not run in the node js
let message = "you are very smart dear" + firstname + secondname;
console.log(message); 